https://localhost {
    tls /etc/caddy/certs/cert.pem /etc/caddy/certs/key.pem
    encode gzip
    reverse_proxy transcendence:3000 {
        header_up X-Real-IP {remote_host}
        header_up X-Forwarded-For {remote_host}
        header_up X-Forwarded-Proto {scheme}
    }
}
